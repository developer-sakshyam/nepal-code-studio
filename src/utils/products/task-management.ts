import { FileEntry } from "../generateProductCode";

export function getTaskManagementFiles(): FileEntry[] {
  return [
    { path: "package.json", content: JSON.stringify({ name: "taskflow-app", version: "1.0.0", private: true, type: "module", scripts: { dev: "vite", build: "tsc && vite build" }, dependencies: { react: "^18.3.0", "react-dom": "^18.3.0", "react-router-dom": "^6.26.0", zustand: "^4.5.0", "date-fns": "^3.6.0", tailwindcss: "^3.4.0", "lucide-react": "^0.400.0" }, devDependencies: { "@types/react": "^18.3.0", "@types/react-dom": "^18.3.0", vite: "^5.4.0", "@vitejs/plugin-react": "^4.3.0", autoprefixer: "^10.4.0", postcss: "^8.4.0" } }, null, 2) },
    { path: "tsconfig.json", content: JSON.stringify({ compilerOptions: { target: "ES2020", module: "ESNext", lib: ["ES2020","DOM","DOM.Iterable"], jsx: "react-jsx", strict: true, moduleResolution: "bundler", allowImportingTsExtensions: true, noEmit: true, paths: { "@/*": ["./src/*"] } }, include: ["src"] }, null, 2) },
    { path: "vite.config.ts", content: `import { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport path from 'path';\nexport default defineConfig({ plugins: [react()], resolve: { alias: { '@': path.resolve(__dirname, './src') } } });` },
    { path: "tailwind.config.js", content: `export default {\n  content: ['./index.html', './src/**/*.{ts,tsx}'],\n  theme: { extend: { colors: { background: 'hsl(var(--background))', foreground: 'hsl(var(--foreground))', primary: { DEFAULT: 'hsl(var(--primary))', foreground: 'hsl(var(--primary-foreground))' }, muted: { DEFAULT: 'hsl(var(--muted))', foreground: 'hsl(var(--muted-foreground))' }, border: 'hsl(var(--border))', card: 'hsl(var(--card))' } } },\n  plugins: []\n};` },
    { path: "postcss.config.js", content: `export default { plugins: { tailwindcss: {}, autoprefixer: {} } };` },
    { path: ".gitignore", content: "node_modules\ndist\n*.log" },
    { path: "index.html", content: `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width, initial-scale=1.0"/><title>TaskFlow</title></head><body><div id="root"></div><script type="module" src="/src/main.tsx"></script></body></html>` },
    { path: "src/main.tsx", content: `import React from 'react';\nimport ReactDOM from 'react-dom/client';\nimport App from './App';\nimport './index.css';\nReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><App /></React.StrictMode>);` },
    { path: "src/index.css", content: `@tailwind base;\n@tailwind components;\n@tailwind utilities;\n\n:root { --background: 222 47% 6%; --foreground: 210 40% 98%; --primary: 160 60% 50%; --primary-foreground: 222 47% 6%; --muted: 222 30% 14%; --muted-foreground: 215 20% 55%; --border: 222 30% 16%; --card: 222 47% 9%; }\nbody { @apply bg-background text-foreground antialiased; }` },
    { path: "src/types.ts", content: `export interface Task {\n  id: string;\n  title: string;\n  description: string;\n  status: 'todo' | 'in-progress' | 'review' | 'done';\n  priority: 'low' | 'medium' | 'high' | 'urgent';\n  assignee?: string;\n  dueDate?: string;\n  labels: string[];\n  createdAt: string;\n}\n\nexport type TaskStatus = Task['status'];\nexport type TaskPriority = Task['priority'];` },
    { path: "src/App.tsx", content: `import { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport { Navbar } from './components/Navbar';\nimport Board from './pages/Board';\nimport ListView from './pages/ListView';\nimport CalendarView from './pages/CalendarView';\n\nexport default function App() {\n  return (\n    <BrowserRouter>\n      <Navbar />\n      <Routes>\n        <Route path="/" element={<Board />} />\n        <Route path="/list" element={<ListView />} />\n        <Route path="/calendar" element={<CalendarView />} />\n      </Routes>\n    </BrowserRouter>\n  );\n}` },
    { path: "src/store/taskStore.ts", content: `import { create } from 'zustand';\nimport { persist } from 'zustand/middleware';\nimport { Task, TaskStatus } from '../types';\n\ninterface TaskStore {\n  tasks: Task[];\n  addTask: (task: Task) => void;\n  updateTask: (id: string, updates: Partial<Task>) => void;\n  deleteTask: (id: string) => void;\n  moveTask: (id: string, status: TaskStatus) => void;\n  searchQuery: string;\n  setSearchQuery: (q: string) => void;\n  filterPriority: string;\n  setFilterPriority: (p: string) => void;\n}\n\nexport const useTaskStore = create<TaskStore>()(\n  persist((set) => ({\n    tasks: [\n      { id: '1', title: 'Design landing page', description: 'Create the hero section, feature grid, and CTA. Follow brand guidelines.', status: 'todo', priority: 'high', assignee: 'Alex', labels: ['design', 'frontend'], createdAt: '2024-03-01', dueDate: '2024-03-15' },\n      { id: '2', title: 'Implement auth system', description: 'Set up JWT authentication with refresh tokens, password reset, and OAuth.', status: 'in-progress', priority: 'urgent', assignee: 'Sarah', labels: ['backend', 'security'], createdAt: '2024-03-02', dueDate: '2024-03-10' },\n      { id: '3', title: 'Write API documentation', description: 'Document all REST endpoints with examples, request/response schemas.', status: 'review', priority: 'medium', assignee: 'Mike', labels: ['docs'], createdAt: '2024-03-03' },\n      { id: '4', title: 'Fix mobile navigation', description: 'Hamburger menu not closing on route change. Test on iOS and Android.', status: 'done', priority: 'low', assignee: 'Alex', labels: ['bug', 'frontend'], createdAt: '2024-02-28' },\n      { id: '5', title: 'Add dark mode toggle', description: 'Implement theme toggle with system preference detection and persistence.', status: 'todo', priority: 'medium', labels: ['frontend', 'design'], createdAt: '2024-03-05' },\n      { id: '6', title: 'Set up CI/CD pipeline', description: 'Configure GitHub Actions for testing, linting, and auto-deployment.', status: 'todo', priority: 'high', assignee: 'Sarah', labels: ['devops'], createdAt: '2024-03-06', dueDate: '2024-03-20' },\n      { id: '7', title: 'Performance audit', description: 'Run Lighthouse, identify bottlenecks, optimize bundle size and images.', status: 'in-progress', priority: 'medium', assignee: 'Mike', labels: ['performance'], createdAt: '2024-03-07' },\n    ],\n    addTask: (task) => set((s) => ({ tasks: [...s.tasks, task] })),\n    updateTask: (id, updates) => set((s) => ({ tasks: s.tasks.map(t => t.id === id ? { ...t, ...updates } : t) })),\n    deleteTask: (id) => set((s) => ({ tasks: s.tasks.filter(t => t.id !== id) })),\n    moveTask: (id, status) => set((s) => ({ tasks: s.tasks.map(t => t.id === id ? { ...t, status } : t) })),\n    searchQuery: '',\n    setSearchQuery: (q) => set({ searchQuery: q }),\n    filterPriority: 'all',\n    setFilterPriority: (p) => set({ filterPriority: p }),\n  }), { name: 'taskflow-storage' })\n);` },
    { path: "src/components/Navbar.tsx", content: `import { NavLink } from 'react-router-dom';\nimport { LayoutGrid, List, Calendar, Plus, Search } from 'lucide-react';\nimport { useState } from 'react';\nimport { AddTaskModal } from './AddTaskModal';\nimport { useTaskStore } from '../store/taskStore';\n\nexport function Navbar() {\n  const [showAdd, setShowAdd] = useState(false);\n  const { searchQuery, setSearchQuery } = useTaskStore();\n\n  return (\n    <>\n      <nav className="border-b border-border bg-card sticky top-0 z-40">\n        <div className="max-w-7xl mx-auto px-4 flex items-center justify-between h-14">\n          <span className="text-lg font-bold text-primary">TaskFlow</span>\n          <div className="flex items-center gap-2">\n            <div className="relative hidden sm:block">\n              <Search className="absolute left-2.5 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" />\n              <input value={searchQuery} onChange={e => setSearchQuery(e.target.value)} placeholder="Search tasks..." className="pl-9 pr-3 py-1.5 rounded-lg bg-muted border border-border text-sm w-48 focus:outline-none focus:border-primary" />\n            </div>\n            <NavLink to="/" className={({ isActive }) => \`p-2 rounded-lg \${isActive ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:text-foreground'}\`}><LayoutGrid className="w-5 h-5" /></NavLink>\n            <NavLink to="/list" className={({ isActive }) => \`p-2 rounded-lg \${isActive ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:text-foreground'}\`}><List className="w-5 h-5" /></NavLink>\n            <NavLink to="/calendar" className={({ isActive }) => \`p-2 rounded-lg \${isActive ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:text-foreground'}\`}><Calendar className="w-5 h-5" /></NavLink>\n            <button onClick={() => setShowAdd(true)} className="px-3 py-1.5 rounded-lg bg-primary text-primary-foreground text-sm font-medium flex items-center gap-1 ml-2"><Plus className="w-4 h-4" /> New</button>\n          </div>\n        </div>\n      </nav>\n      {showAdd && <AddTaskModal onClose={() => setShowAdd(false)} />}\n    </>\n  );\n}` },
    { path: "src/components/AddTaskModal.tsx", content: `import { useState } from 'react';\nimport { X } from 'lucide-react';\nimport { useTaskStore } from '../store/taskStore';\nimport { TaskPriority, TaskStatus } from '../types';\n\nexport function AddTaskModal({ onClose }: { onClose: () => void }) {\n  const addTask = useTaskStore(s => s.addTask);\n  const [title, setTitle] = useState('');\n  const [desc, setDesc] = useState('');\n  const [priority, setPriority] = useState<TaskPriority>('medium');\n  const [status, setStatus] = useState<TaskStatus>('todo');\n  const [assignee, setAssignee] = useState('');\n  const [dueDate, setDueDate] = useState('');\n\n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    if (!title.trim()) return;\n    addTask({\n      id: Date.now().toString(),\n      title, description: desc, status, priority,\n      assignee: assignee || undefined,\n      dueDate: dueDate || undefined,\n      labels: [],\n      createdAt: new Date().toISOString().split('T')[0]\n    });\n    onClose();\n  };\n\n  return (\n    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm" onClick={onClose}>\n      <form onSubmit={handleSubmit} onClick={e => e.stopPropagation()} className="w-full max-w-md p-6 rounded-2xl bg-card border border-border">\n        <div className="flex items-center justify-between mb-6"><h2 className="text-lg font-bold">New Task</h2><button type="button" onClick={onClose}><X className="w-5 h-5 text-muted-foreground" /></button></div>\n        <div className="space-y-4">\n          <input value={title} onChange={e => setTitle(e.target.value)} placeholder="Task title *" required className="w-full px-4 py-2.5 rounded-xl bg-muted border border-border text-sm focus:outline-none focus:border-primary" />\n          <textarea value={desc} onChange={e => setDesc(e.target.value)} placeholder="Description" rows={3} className="w-full px-4 py-2.5 rounded-xl bg-muted border border-border text-sm resize-none focus:outline-none focus:border-primary" />\n          <div className="grid grid-cols-2 gap-3">\n            <div>\n              <label className="text-xs text-muted-foreground mb-1 block">Priority</label>\n              <select value={priority} onChange={e => setPriority(e.target.value as TaskPriority)} className="w-full px-3 py-2 rounded-xl bg-muted border border-border text-sm">\n                <option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="urgent">Urgent</option>\n              </select>\n            </div>\n            <div>\n              <label className="text-xs text-muted-foreground mb-1 block">Status</label>\n              <select value={status} onChange={e => setStatus(e.target.value as TaskStatus)} className="w-full px-3 py-2 rounded-xl bg-muted border border-border text-sm">\n                <option value="todo">To Do</option><option value="in-progress">In Progress</option><option value="review">Review</option><option value="done">Done</option>\n              </select>\n            </div>\n          </div>\n          <div className="grid grid-cols-2 gap-3">\n            <div><label className="text-xs text-muted-foreground mb-1 block">Assignee</label><input value={assignee} onChange={e => setAssignee(e.target.value)} placeholder="Name" className="w-full px-3 py-2 rounded-xl bg-muted border border-border text-sm" /></div>\n            <div><label className="text-xs text-muted-foreground mb-1 block">Due Date</label><input type="date" value={dueDate} onChange={e => setDueDate(e.target.value)} className="w-full px-3 py-2 rounded-xl bg-muted border border-border text-sm" /></div>\n          </div>\n        </div>\n        <button type="submit" className="w-full py-2.5 rounded-xl bg-primary text-primary-foreground font-medium mt-6">Create Task</button>\n      </form>\n    </div>\n  );\n}` },
    { path: "src/components/TaskCard.tsx", content: `import { Task } from '../types';\nimport { Calendar, User, Trash2, GripVertical } from 'lucide-react';\nimport { format } from 'date-fns';\nimport { useTaskStore } from '../store/taskStore';\n\nconst priorityConfig: Record<string, { color: string; bg: string }> = {\n  low: { color: 'text-blue-400', bg: 'bg-blue-500' },\n  medium: { color: 'text-yellow-400', bg: 'bg-yellow-500' },\n  high: { color: 'text-orange-400', bg: 'bg-orange-500' },\n  urgent: { color: 'text-red-400', bg: 'bg-red-500' },\n};\n\nexport function TaskCard({ task }: { task: Task }) {\n  const deleteTask = useTaskStore(s => s.deleteTask);\n  const config = priorityConfig[task.priority];\n\n  return (\n    <div\n      draggable\n      onDragStart={(e) => e.dataTransfer.setData('taskId', task.id)}\n      className="p-4 rounded-xl bg-card border border-border hover:border-primary/30 cursor-grab active:cursor-grabbing transition-all group"\n    >\n      <div className="flex items-center justify-between mb-2">\n        <div className="flex items-center gap-2">\n          <div className={\`w-2 h-2 rounded-full \${config.bg}\`} />\n          <span className={\`text-xs font-medium capitalize \${config.color}\`}>{task.priority}</span>\n        </div>\n        <button onClick={() => deleteTask(task.id)} className="opacity-0 group-hover:opacity-100 p-1 rounded hover:bg-destructive/10 text-destructive transition-all"><Trash2 className="w-3.5 h-3.5" /></button>\n      </div>\n      <h4 className="font-medium text-sm mb-1">{task.title}</h4>\n      <p className="text-xs text-muted-foreground line-clamp-2 mb-3">{task.description}</p>\n      {task.labels.length > 0 && <div className="flex gap-1 mb-3 flex-wrap">{task.labels.map(l => <span key={l} className="px-2 py-0.5 rounded-full bg-muted text-xs">{l}</span>)}</div>}\n      <div className="flex items-center justify-between text-xs text-muted-foreground">\n        {task.dueDate ? <span className="flex items-center gap-1"><Calendar className="w-3 h-3" />{format(new Date(task.dueDate), 'MMM d')}</span> : <span />}\n        {task.assignee && <span className="flex items-center gap-1"><User className="w-3 h-3" />{task.assignee}</span>}\n      </div>\n    </div>\n  );\n}` },
    { path: "src/components/KanbanColumn.tsx", content: `import { Task, TaskStatus } from '../types';\nimport { TaskCard } from './TaskCard';\n\nconst statusConfig: Record<string, { label: string; color: string }> = {\n  'todo': { label: 'To Do', color: 'bg-blue-500' },\n  'in-progress': { label: 'In Progress', color: 'bg-yellow-500' },\n  'review': { label: 'Review', color: 'bg-purple-500' },\n  'done': { label: 'Done', color: 'bg-emerald-500' },\n};\n\nexport function KanbanColumn({ status, tasks, onDrop }: { status: TaskStatus; tasks: Task[]; onDrop: (taskId: string) => void }) {\n  const config = statusConfig[status];\n  return (\n    <div className="flex-1 min-w-[280px]"\n      onDragOver={(e) => { e.preventDefault(); e.currentTarget.classList.add('bg-primary/5'); }}\n      onDragLeave={(e) => { e.currentTarget.classList.remove('bg-primary/5'); }}\n      onDrop={(e) => {\n        e.currentTarget.classList.remove('bg-primary/5');\n        const id = e.dataTransfer.getData('taskId');\n        if (id) onDrop(id);\n      }}\n      className="rounded-xl p-3 transition-colors"\n    >\n      <div className="flex items-center gap-2 mb-4 px-1">\n        <div className={\`w-2.5 h-2.5 rounded-full \${config.color}\`} />\n        <h3 className="font-semibold text-sm">{config.label}</h3>\n        <span className="text-xs text-muted-foreground bg-muted px-2 py-0.5 rounded-full ml-auto">{tasks.length}</span>\n      </div>\n      <div className="space-y-3">\n        {tasks.map(task => <TaskCard key={task.id} task={task} />)}\n        {tasks.length === 0 && <div className="text-center py-8 text-xs text-muted-foreground border border-dashed border-border rounded-xl">Drop tasks here</div>}\n      </div>\n    </div>\n  );\n}` },
    { path: "src/pages/Board.tsx", content: `import { useTaskStore } from '../store/taskStore';\nimport { KanbanColumn } from '../components/KanbanColumn';\nimport { TaskStatus } from '../types';\nimport { useMemo } from 'react';\n\nconst statuses: TaskStatus[] = ['todo', 'in-progress', 'review', 'done'];\n\nexport default function Board() {\n  const { tasks, moveTask, searchQuery, filterPriority } = useTaskStore();\n\n  const filtered = useMemo(() => {\n    return tasks.filter(t => {\n      if (searchQuery && !t.title.toLowerCase().includes(searchQuery.toLowerCase())) return false;\n      if (filterPriority !== 'all' && t.priority !== filterPriority) return false;\n      return true;\n    });\n  }, [tasks, searchQuery, filterPriority]);\n\n  return (\n    <div className="max-w-7xl mx-auto px-4 py-6">\n      <div className="flex items-center justify-between mb-6">\n        <h1 className="text-2xl font-bold">Board</h1>\n        <div className="flex gap-2">\n          {['all', 'urgent', 'high', 'medium', 'low'].map(p => (\n            <button key={p} onClick={() => useTaskStore.getState().setFilterPriority(p)}\n              className={\`px-3 py-1 rounded-lg text-xs capitalize \${filterPriority === p ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground'}\`}>{p}</button>\n          ))}\n        </div>\n      </div>\n      <div className="flex gap-4 overflow-x-auto pb-4">\n        {statuses.map(status => (\n          <KanbanColumn key={status} status={status} tasks={filtered.filter(t => t.status === status)} onDrop={(id) => moveTask(id, status)} />\n        ))}\n      </div>\n    </div>\n  );\n}` },
    { path: "src/pages/ListView.tsx", content: `import { useTaskStore } from '../store/taskStore';\nimport { Trash2 } from 'lucide-react';\nimport { useMemo } from 'react';\nimport { TaskStatus } from '../types';\n\nconst priorityColors: Record<string, string> = { low: 'text-blue-400', medium: 'text-yellow-400', high: 'text-orange-400', urgent: 'text-red-400' };\nconst statusColors: Record<string, string> = { todo: 'bg-blue-500/10 text-blue-400', 'in-progress': 'bg-yellow-500/10 text-yellow-400', review: 'bg-purple-500/10 text-purple-400', done: 'bg-emerald-500/10 text-emerald-400' };\n\nexport default function ListView() {\n  const { tasks, deleteTask, moveTask, searchQuery } = useTaskStore();\n  const filtered = useMemo(() => tasks.filter(t => !searchQuery || t.title.toLowerCase().includes(searchQuery.toLowerCase())), [tasks, searchQuery]);\n\n  return (\n    <div className="max-w-5xl mx-auto px-4 py-6">\n      <h1 className="text-2xl font-bold mb-6">All Tasks ({filtered.length})</h1>\n      <div className="rounded-xl border border-border overflow-hidden">\n        <table className="w-full">\n          <thead><tr className="bg-muted/50 border-b border-border">\n            <th className="text-left p-3 text-sm font-medium text-muted-foreground">Task</th>\n            <th className="text-left p-3 text-sm font-medium text-muted-foreground">Priority</th>\n            <th className="text-left p-3 text-sm font-medium text-muted-foreground">Status</th>\n            <th className="text-left p-3 text-sm font-medium text-muted-foreground">Assignee</th>\n            <th className="p-3 w-12"></th>\n          </tr></thead>\n          <tbody>\n            {filtered.map(task => (\n              <tr key={task.id} className="border-b border-border/50 hover:bg-muted/30 transition-colors">\n                <td className="p-3"><p className="font-medium text-sm">{task.title}</p><p className="text-xs text-muted-foreground line-clamp-1">{task.description}</p></td>\n                <td className="p-3"><span className={\`text-sm capitalize font-medium \${priorityColors[task.priority]}\`}>{task.priority}</span></td>\n                <td className="p-3">\n                  <select value={task.status} onChange={e => moveTask(task.id, e.target.value as TaskStatus)} className={\`text-xs rounded-lg px-2.5 py-1 border-0 font-medium \${statusColors[task.status]}\`}>\n                    <option value="todo">To Do</option><option value="in-progress">In Progress</option><option value="review">Review</option><option value="done">Done</option>\n                  </select>\n                </td>\n                <td className="p-3 text-sm text-muted-foreground">{task.assignee || '—'}</td>\n                <td className="p-3"><button onClick={() => deleteTask(task.id)} className="p-1.5 text-destructive hover:bg-destructive/10 rounded-lg"><Trash2 className="w-4 h-4" /></button></td>\n              </tr>\n            ))}\n          </tbody>\n        </table>\n      </div>\n    </div>\n  );\n}` },
    { path: "src/pages/CalendarView.tsx", content: `import { useTaskStore } from '../store/taskStore';\nimport { useMemo } from 'react';\nimport { format, startOfMonth, endOfMonth, eachDayOfInterval, isSameDay, isToday } from 'date-fns';\n\nconst priorityDots: Record<string, string> = { low: 'bg-blue-500', medium: 'bg-yellow-500', high: 'bg-orange-500', urgent: 'bg-red-500' };\n\nexport default function CalendarView() {\n  const tasks = useTaskStore(s => s.tasks);\n  const now = new Date();\n  const days = eachDayOfInterval({ start: startOfMonth(now), end: endOfMonth(now) });\n  const startDay = days[0].getDay();\n\n  const tasksByDate = useMemo(() => {\n    const map = new Map<string, typeof tasks>();\n    tasks.filter(t => t.dueDate).forEach(t => {\n      const key = t.dueDate!;\n      if (!map.has(key)) map.set(key, []);\n      map.get(key)!.push(t);\n    });\n    return map;\n  }, [tasks]);\n\n  return (\n    <div className="max-w-4xl mx-auto px-4 py-6">\n      <h1 className="text-2xl font-bold mb-2">Calendar</h1>\n      <p className="text-muted-foreground mb-6">{format(now, 'MMMM yyyy')}</p>\n      <div className="grid grid-cols-7 gap-1">\n        {['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'].map(d => <div key={d} className="text-center text-xs text-muted-foreground font-medium py-2">{d}</div>)}\n        {Array.from({ length: startDay }, (_, i) => <div key={\`empty-\${i}\`} />)}\n        {days.map(day => {\n          const dateStr = format(day, 'yyyy-MM-dd');\n          const dayTasks = tasksByDate.get(dateStr) || [];\n          return (\n            <div key={dateStr} className={\`min-h-[80px] p-1.5 rounded-lg border \${isToday(day) ? 'border-primary bg-primary/5' : 'border-border/50'}\`}>\n              <span className={\`text-xs font-medium \${isToday(day) ? 'text-primary' : 'text-muted-foreground'}\`}>{format(day, 'd')}</span>\n              <div className="mt-1 space-y-0.5">\n                {dayTasks.slice(0, 2).map(t => (\n                  <div key={t.id} className="flex items-center gap-1 px-1 py-0.5 rounded bg-muted/50">\n                    <div className={\`w-1.5 h-1.5 rounded-full \${priorityDots[t.priority]}\`} />\n                    <span className="text-[10px] truncate">{t.title}</span>\n                  </div>\n                ))}\n                {dayTasks.length > 2 && <span className="text-[10px] text-muted-foreground px-1">+{dayTasks.length - 2} more</span>}\n              </div>\n            </div>\n          );\n        })}\n      </div>\n    </div>\n  );\n}` },
    { path: "README.md", content: `# TaskFlow - Task Management App\n\nA Kanban-style project management app with board, list, and calendar views.\n\n## 🚀 Getting Started\n\n\`\`\`bash\nnpm install\nnpm run dev\n\`\`\`\n\n## ✨ Features\n\n- 📋 **Kanban Board** — Drag-and-drop columns (To Do, In Progress, Review, Done)\n- 📝 **List View** — Sortable table with inline status changes\n- 📅 **Calendar View** — Monthly calendar with task due dates\n- ➕ **Task Creation** — Modal with priority, assignee, due date, and status\n- 🔍 **Search & Filter** — Real-time search and priority filters\n- 💾 **Persistent** — Zustand + localStorage for data persistence\n- 🏷️ **Labels & Priority** — Color-coded priority levels and task labels\n- 🗑️ **Delete Tasks** — Remove tasks from any view\n- 📱 **Responsive** — Mobile-friendly layout\n\n## 🛠 Tech Stack\n\nReact 18 • TypeScript • Tailwind CSS • Zustand • date-fns • React Router v6 • Lucide Icons\n\nBuilt with ❤️ by Sakshyam Kharel | CodeVault` },
  ];
}
